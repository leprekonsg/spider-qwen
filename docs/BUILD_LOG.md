# Build Log — spider-qwen (SPIDER_QWEN_BUILD_PLAN execution)

One line per completed task. TDD: failing test(s) first, then implementation, then
the guardrail gate (offline e2e + full suite green) before commit.

| Task | Status | Notes |
|---|---|---|
| T-0 | DONE | `models:` block in policy_config.yaml (planner/extraction/embeddings/ocr) + `Policy.model_for(role)` with `SPIDER_QWEN_MODEL_<ROLE>` env overrides; legacy `qwen.*_model` keys and `QWEN_*` env still honoured. 9 new tests (test_models_config.py). Offline run green; 75 tests pass. |
| T-1.1 | DONE | `ranking/serendipity.py`: 4-slot `SerendipityResult` (primary_answer, s1_substitutes, s2_long_tail_sources, s3_risk_signals, evidence_refs, serendipity_score). Deterministic relevance x novelty x unexpectedness scoring vs top-3 baseline; `RiskSignal` model + `extra_risk_signals` seam for later phases. Wired into `RunResult.serendipity` via controller. 10 new tests; 85 pass. |
| T-1.2 | DONE | `serendipity/query_rewrite.py` `expand_query()` -> Step-Back + HyDE + Query2Doc + obsolescence-vocab + MPN-pattern + broker-operator variants (deterministic/offline; optional Qwen HyDE). `Planner.expand_query` passthrough. Golden test on "16-pin SMT op-amp TI..." passes. 6 new tests; 91 pass. |
| T-1.3 | DONE | `serendipity/corrective.py` CRAG `evaluate_retrieval()` (content-coverage + procurement-signal heuristic -> correct/ambiguous/incorrect) + page condensation; `corrective_queries()` (broker-first). Controller refactored to `_gather_queries`; on `incorrect` verdict it broaden/broker-pivots instead of answering. Tracer gains `detail`; metrics expose crag_verdict/confidence/corrective_searches. 7 new tests; 98 pass. |
| T-1.4 | DONE | `agent/compiler.py`: `TokenBucket` + `RateLimiter` (5 search/min, 25 fetch/min, injectable clock) + `LLMCompiler` DAG executor (level-parallel `asyncio.gather`, cycle/unknown-dep checks, per-kind peak-concurrency + DAG trace). Controller gains `rate_limiter`/`compiler` and `gather_parallel` (used by T-3.3) on a shared `_fetch_and_extract`. 8 new tests; 106 pass. **Phase 1 checkpoint:** offline hero query runs end-to-end (serendipity slots + CRAG verdict). Follow-up `fix(phase-1)`: wired query expansion (`merge_gather_queries` interleaves geo+expansion) and concurrent fetch (compiler DAG) into the **primary** gather path (were T-3.3-only); honest scaffold labeling for T-1.1 slots (s1/s2 are rank positions, not true S1/S2 classification). This routed the offline path through the rate-limited compiler and exposed a regression: the wall-clock `TokenBucket` (5 search/min) throttled the 80-case offline benchmark to ~45s/case (~1h, looked like a hang). Fix (SRP/DRY — offline-ness already expressed once, by the mock providers): providers carry `rate_limited` (mock=False; live TinyFish/Qwen=True); `Controller._build_rate_limiter()` picks `NullRateLimiter` when no provider draws a live quota, real `RateLimiter` otherwise (unknown providers default to throttled, protecting the live API). 2 regression tests (offline no-throttle timing guard + live-quota still-throttles). 165 pass. |
| T-2.1 | DONE | **Schema-constrained extraction + judge gate.** `tools/qwen_json_extractor.py`: re-prompt-on-malformed retry loop (`max_retries=2`) feeding the validation error back to qwen3.5-flash + retry telemetry (`calls/retries/malformed_final/last_attempts`, `retry_rate()`); malformed output recovers to a valid schema, post-retry malformed rate ~0. New `tools/page_judge.py`: Prometheus-style `PageJudge` scoring each fetched page on relevance (content-term coverage) x freshness (year recency) x source-authority (mfr/dist/aggregator/broker/marketplace/gov/business tiers) minus contradicts-ledger (disjoint price vs prior same-host item) -> `accept|flag|reject` (deterministic; optional Qwen `llm` override seam). Gate wired at the persistence boundary in `FetchService` (judge before `ledger.record`): reject -> dropped from results + not stored (recorded to errors), flag -> stored with `gate_status`/`judge` metadata + confidence 0.4, accept -> stored + metadata. Judges every page incl. empty-text (image-only low-authority pages cannot slip the gate). Controller gains `page_judge` param + `policy.qwen_page_judge_enabled()` (off by default; offline pipeline unchanged) + metrics `pages_rejected`/`pages_flagged`. Design note: judge lives in its own module and gates in FetchService (the page ledger-writer) rather than mutating `ledger.py` — the ledger already persists the verdict via existing metadata, so no `ledger.py` change was needed. 21 new tests (retry/judge/gate incl. controller e2e + empty-text regression); adversarial review (22 agents, 18 findings -> 1 confirmed+fixed). 127 pass. **Phase 2 begins.** Follow-up `fix(security)`: page judge LLM override hardened against prompt injection (untrusted page text) — override may only TIGHTEN the verdict, numeric fields type-checked + clamped, prompt isolates title/text in delimited blocks with refusal instructions; 4 tests. 131 pass. |
| T-2.2 | DONE | **Verification spine.** New `verification/atomic.py` (`decompose()` -> `AtomicClaim`s: vendor/price/moq/quote_channel/contact; critical flag on the mode-defining fact, conservative `not in _NON_PRICED` so a populated price with unknown status stays critical), `verification/minicheck.py` (MiniCheck entailment gatekeeper: value-grounding with currency/space/comma normalization + **whole-number numeric match** so "129" is not grounded by "1290"; token-overlap fallback; optional learned-NLI `model` seam, type-checked/clamped/never-raises), `verification/safe.py` (`SafeReverifier`: re-grounds flagged atoms against the wider evidence corpus + optional `search_fn`/TinyFish seam). `evidence/verifier.py` gains `VerificationSpine` (decompose -> MiniCheck -> SAFE): grounds each claim against the **source page text** (title+body via `parent_ledger_id`), **never the self-referential extraction snippet** (semantic_memory/synthetic refs return no premise -> forced through SAFE), writes `verified`/`verifier_score`/`verifier_stage` onto each claim ledger row; candidate `verified` iff all critical claims hold. Controller gains `verify`/`minicheck` params + `policy.verification_enabled()` (off by default; offline pipeline unchanged), blocks unverified candidates from `validated_candidates`, adds metrics `claims_verified`/`claims_unsupported`/`candidates_blocked_unverified`. Acceptance: injected fabricated price (S$999, not in page) -> critical price claim `verified=false` -> candidate blocked; paired no-verify test shows it would otherwise reach output. 22 new tests (atomic/minicheck/safe/spine + controller e2e + numeric-boundary/currency-regex/memory-self-reference/unknown-status regressions); adversarial review (36 agents, 30 findings -> 4 real bugs confirmed+fixed: numeric-substring bypass, semantic-memory self-grounding, price-criticality on unknown status, currency-regex mid-word). v1 limitations (documented, deferred): SAFE live-search + MiniCheck-FT5 model are opt-in seams (deterministic/offline default per project convention); currency/unit not yet separate critical claims; serendipity discovery view intentionally pre-verification (substitute verification is T-2.3/CoVe). 153 pass. Offline e2e green off + on (10/10 claims verified, 0 false blocks). Follow-up `fix(T-2.2)`: relation-grounding — vendor-scoped critical claims (price/moq/quote_channel + `contact_*`) now require the value and vendor to co-occur in one sentence (or the line above a price), so a competitor's price elsewhere on the page cannot verify the claim. `MiniCheck.check(subject=...)` + `_relation_grounded`; `VerificationSpine._minicheck_subject`; SAFE re-verify passes `claim.subject`. test_verification_spine extended. |
| T-2.3 | DONE | **CoVe + semantic entropy + contradiction.** New `verification/cove.py`: `SubstituteCandidate` (mpn/manufacturer/lifecycle_status/evidence_refs) + `ChainOfVerification` (draft -> fixed verify-questions answered against each candidate's evidence-grounded attributes, NOT the draft's self-assertion -> revise: drop any candidate failing a critical question) so a plausible-but-discontinued ("phantom") substitute is removed even at self-asserted confidence 0.99; `semantic_entropy(samples)` clusters on normalized (MPN, manufacturer) -> confidence = 1 - normalized entropy (agree=1.0, scatter~0.0); optional `sampler` seam draws N samples per candidate (Qwen generator) and gates on `min_confidence` (off by default). Contradiction -> `disputed` reuses semantic memory (DRY, per decision): `evidence/models.py` gains `DisputedAlternative` (value+confidence+spans); `memory/promotion.py` gains `contradicts()` (case/space/punct-normalized inequality; also fixes case-variant values being wrongly disputed); `memory/semantic.py` `SemanticFact` gains `entity_type="part"` + `disputed_alternatives`, and `upsert` now retains EVERY conflicting side (highest-confidence -> primary value/refs, rest -> alternatives) instead of discarding the loser. Acceptance: a Mouser-active vs DigiKey-NRND pair -> one `disputed` record keeping both values + both spans (survives reload); a phantom EOL substitute removed by CoVe. Standalone + opt-in (not wired into the controller yet; substitutes are populated by the parts graph/Wayback in T-3.x/T-5.x). 16 new tests (test_cove.py, test_contradiction.py). 181 pass. **T-2.4 next.** Follow-up `fix(T-2.3)`: removed the confidence-dominance overwrite in `SemanticMemory.upsert` -- a clearly-more-confident contradicting claim (gap > 0.1) was silently replacing the weaker fact (status stayed `active`, weaker span discarded), contradicting "contradiction -> disputed, retains both spans." Now ANY cross-source contradiction is disputed (highest-confidence side becomes primary value/refs, weaker side retained as a `disputed_alternative`). +1 regression test (high-confidence-gap dispute). |
| T-2.4 | DONE | **Source-reliability prior + Merkle-DAG ledger.** New `governance/source_reliability.py` is the single home for source tiering: `classify_source(host)` (manufacturer/distributor/aggregator/broker/marketplace/government/business/unknown via suffix-matched domain lists) + `DEFAULT_RELIABILITY` priors (mfr 0.99, auth dist 0.95, aggregator 0.85, business 0.6, broker 0.5, unknown 0.4, marketplace 0.2) + `reliability_for(url, priors=)`. `tools/page_judge.py` now imports the shared classifier (DRY: removed its duplicate domain lists / `_classify_source` / `_host`; keeps its own gate `_AUTHORITY` weights). `evidence/models.py` `EvidenceItem` gains `reliability` + `parent_hash`/`chain_hash` + `content_digest()` (canonical digest over every field except the chain links) + `effective_confidence` property (= confidence x reliability; base confidence is never mutated). `evidence/ledger.py`: `record()` classifies the source, stores its reliability, then links each row `chain_hash = sha256(content_digest || parent_chain_hash)`; `verify_chain()` re-walks and flags the exact tampered/mis-linked `ledger_id`; `load()` resumes the chain tip so a later append continues the chain. Priors are policy-driven: `policy_config.yaml` `source_reliability:` block (overrides the code defaults) -> `Policy.source_reliability()` -> injected into the ledger by the controller. `evidence verify <run_id>` CLI now reports the chain re-walk (chain_ok/chain_checked/chain_issues) alongside span verification; exit 0 only if both hold. Acceptance: tampering one persisted row -> `verify_chain` not ok and points at that `ledger_id`; manufacturer effective confidence > marketplace. Cycle note: ledger imports `source_reliability` lazily inside `record()` (governance/__init__ pulls modes.contracts -> evidence.models). 9 new tests (test_reliability_ledger.py). 190 pass. **Phase 2 complete.** |
| T-3.1 | DONE | **Supplier-part property graph (SQLite LPG).** New `graph/` package: `schema.py` (two-table DDL -- `nodes(id,type,props)` + `edges(src,dst,rel,confidence,reliability,evidence_claim_id,event_ts,ingest_ts,grade,props)` with PK `(src,dst,rel,evidence_claim_id)` = one edge per asserting claim + src/dst indexes; node-type & relation-type tuples; canonical key helpers `part_key/mfr_key/dist_key`), `store.py` (`GraphStore` over stdlib `sqlite3`, file or `:memory:`; `upsert_node` merges props, `add_edge` rejects an empty evidence_claim_id (hard rule: never a bare URL) and dedups on the PK, `neighbors`, `traverse` = bounded multi-hop **recursive CTE**, `edges`/`node_count`/`edge_count`), `extract.py` (REBEL/SAC-KG shape: Generator = per-sentence relation-phrase + nearest-entity triples over MPN / manufacturer / distributor surfaces; Verifier = MiniCheck grounds each object surface in the source text (T-2.2 spine) so a hallucinated relation cannot upsert; Pruner = drop self-loops + duplicate triples; `ingest_text` upserts verified triples carrying the page's `evidence_claim_id` + source reliability). `evidence/graph.py` gains `render_property_graph(store)` Mermaid renderer (reuses `_node_id`/`_escape`). One embedded file -- no service, no Docker, `--offline` stays network-free; sqlite-vec embeddings can live in the same file (commented in the DDL). Cycle-safe: store imports only `schema` + stdlib. Standalone + opt-in (controller/retrieval integration is T-3.2). Acceptance: ingesting fixture datasheet prose builds a graph where the recursive-CTE traversal from `part:atmega48` resolves ACQUIRED_BY Microchip + SUPERSEDED_BY ATMEGA48A (+ CROSS_REFERENCE PIC16F88) within <=2 hops. 11 new tests (test_graph.py). 201 pass. **Phase 3 begins.** |
| T-3.2 | DONE | **HippoRAG PPR multi-hop retrieval.** New `graph/retrieve.py`: `personalized_pagerank(adjacency, seeds)` (pure-Python power iteration with restart/teleport to seeds + dangling-mass redistribution -- no scipy/numpy, keeps `--offline` light), `GraphRetriever` (seeds = query MPNs via `extract.find_mpns`; PPR over edges weighted by confidence x reliability; each result carries a Reasoning-on-Graphs relation path from a seed that doubles as the buyer-facing explanation), and `flat_vector_rank` (deliberately flat lexical baseline for the comparison). `extract.find_mpns` exposed (shared seed/extraction MPN detection, DRY). `controller.graph_retrieve(query, ledger)` builds the LPG from a run's ledger page text (every edge -> page `ledger_id` + source reliability) and runs PPR -- opt-in, not in the default pipeline. Acceptance: a multi-hop substitute query returns the successor with path "... -> SUPERSEDED_BY -> ..."; PPR recall@5 = 1.0 vs flat-baseline 0.0 on a 10-query fixture (successors are graph-adjacent but lexically dissimilar, so the flat baseline misses them). 4 new tests (test_graph_retrieve.py). 205 pass. |
| T-3.3 | DONE | **GRAM-lite Serendipity Mode.** New `serendipity/gram_lite.py`: `sample_trajectories` draws S=5 diverse trajectories (synonym / broader-class / broker-operator / SEA-local-language / mfr-alt-PN) by bucketing the T-1.2 expansion (DRY) + a SEA-local hint; `run_serendipity` is a width-first, depth-capped (D_MAX=3) recursion with per-hop confidence x0.85, **budget-bounded by hard caps** (<=1 Max + <=25 flash + <=45 fetch per top-level query) enforced by construction -- it stops before crossing a cap, so `within_caps` is always true; every recursion edge records `(parent_sha, child_sha, depth, query, category, confidence, ts)`; verification fan-out K=3 (refetch / competing_vendor / wayback) routes any disagreement to the disputed handler (T-2.3). `fetch_fn`/`verify_fn`/`llm`/`disputed_handler` are injectable seams (deterministic + network-free by default). `modes/router.py` `RoutePlan` gains `serendipity_mode` (True for PRODUCT_EXACT_PRICE = substitute discovery + REVALIDATION). Standalone + opt-in (not auto-run in the default pipeline). Acceptance: substitute & revalidation routes flag serendipity_mode; the result exposes the recursion tree with multi-hop edges to depth 3; cost within caps; disagreeing verifiers route to the disputed handler. 7 new tests (test_gram_lite.py). 212 pass. **Phase 3 complete.** Follow-up `fix(T-3.3)`: under default caps the width-first BFS spent the entire 45-fetch budget on discovery, so the K=3 verification fan-out never ran (probe: edges=45, leaf_count=37, verifications=0) -- contradicting the "K=3 verify -> disputed" claim on the normal path. Fix: hold back a `VERIFY_K * VERIFY_LEAVES` fetch reserve so discovery cannot starve verification, and traverse depth-first (a stack) so `D_MAX` is still reached within the smaller discovery budget (BFS spent it all on breadth before reaching depth 3). The fan-out now runs on the default path; `within_caps` still holds. +1 regression test (default-cap verification). 214 pass. |
| T-4.1 | DONE | **Reflections + MemoryBank decay + A-MEM Zettelkasten notes.** `memory/decay.py` now models Ebbinghaus forgetting `exp(-age/S)` via `memory_stability_days(fact)`: base `S = half_life/ln2` (so the unreinforced curve is identical to the old `0.5**(age/half_life)` -- a one-half-life-old fact still decays to exactly 0.5), grown by `reinforcement_count` (spaced repetition) and halved per contradiction (one `disputed_alternative` = stability/2, disputed facts forget faster). `SemanticFact` gains `reinforcement_count` (backward-compatible default 0); `SemanticMemory.upsert` increments it on each corroborating (agreeing) observation = a re-access. New `memory/reflections.py` `ReflectionEngine.reflect(facts, episodes, max_insights=10)` distils deterministic, evidence-cited insights (dispute -> staleness -> best-corroboration -> coverage -> run-outcome), capped; optional `llm` summary seam OFF by default. New `memory/notes.py` `NoteStore.add_from_fact` writes a Zettelkasten note per fact tagged entity/geo (SEA keyword map)/supply-stage (field->sourcing|pricing|lifecycle|contact)/risk-flag (disputed or NRND/EOL/obsolete token) with 1-3 agentic links scored by shared entity(3)>stage(1)>geo(1)>risk(1); JSONL backend (Mem0g/Qdrant deferred). CLI `memory show` now also prints `stability_days`; new read-only `memory reflect` / `memory notes` views. Acceptance: `memory show` prints TTL/last_verified/confidence (existing); decay grows on re-access + shrinks per contradiction; reflections distil capped insights; notes carry attributes + links and survive reload. The cross-run cached-quote-channel reuse the acceptance also names was already implemented (controller `_apply_memory_recalls` + `memory_recall` trace). 9 new tests (test_memory_phase4.py). 223 pass. **Phase 4 begins.** |
| T-4.2 | DONE | **Voyager skill library + Agent Workflow Memory + AutoGuide.** New `memory/skills_library.py`: a successful query is "skeletonized" (region words -> `{region}`, integers -> `{number}`); when the same `(mode, skeleton)` recurs across >= `min_support` (default 3) episodes, `SkillLibrary.induce_from_episodes` stores one parameterized `Skill` (template + `parameters` placeholders + `support` + source episode ids). `retrieve` skeletonizes the incoming query -> exact-skeleton match, else Jaccard lexical fallback over masked tokens (>=0.5); an injectable `embedder` (text-embedding-v4) seam switches retrieval to cosine and is OFF by default. New `memory/workflows.py`: `WorkflowMemory.induce(trajectories, mode, min_support)` derives the longest common step *prefix* shared by all trajectories into a reusable `Workflow` playbook; AutoGuide `add_guideline(context, advice)` + `guidelines_for(context)` return context-conditional tips when the trigger token appears in the current context. Plain JSONL persistence; deterministic, network-free; standalone + opt-in (not auto-wired into the controller). Acceptance: 3 "ISO-certified supplier in region R, MOQ < N" service episodes induce one skill retrieved for a 4th new-region query (survives reload); below support nothing is induced; AWM induces the shared step prefix; guidelines fire on broker/EOL context. 6 new tests (test_skills_workflows.py). 229 pass. **Phase 4 (memory moat) complete.** |
| (v2 amendment) | NOTE | Reviewed a GRAM-inspired "Build Plan v2" (arXiv:2605.19376 borrowed as a principle, not ported). Decision (user-confirmed): **reasoning-first, hybrid storage; keep v1 IDs + amend.** ~9 v2 tasks were already DONE under v1 IDs. Adopt v2's reasoning spine (trajectories + Process Reward Model + recursive repair + winner selection + provenance) as new tasks T-R.1..T-R.4; reframe T-4.3 to bi-temporal SQLite columns (Zep/Graphiti dropped); make sqlite-vec/vector recall an OPTIONAL seam (offline lexical default, auto-fallback); add electronics_substitution vertical. **Dropped:** v1 T-5.3 LinUCB bandit (RL source-selection conflicts with the no-RL ethos); T-6.2 EOL forecaster + counterfactual-BOM/OOD demoted to NICE. Rejected the full single-file sqlite-vec migration (rewinds verified work, non-portable extension-loading dep, low offline gain). Probe: this box is Python 3.13.9 Anaconda and CAN load SQLite extensions, but that is not portable to demo/CI. See SPIDER_QWEN_BUILD_PLAN.md "v2 amendment". |
| T-R.1 | DONE | **Reasoning trajectories + Process Reward Model.** New `reasoning/` package (GRAM principle, not ported; deterministic + network-free). `reasoning/trajectory.py`: `TrajectoryStrategy` enum (5 service: official-site/quote-channel/directory-contact/SEA-local-phrasing/category-pricing; 5 electronics: authorized-dist/FFF-substitute/lifecycle-PCN/broker-long-tail/archived-datasheet), `strategies_for(mode)` (service modes -> service set, else electronics), `ReasoningTrajectory` + `BundleMetrics` (normalized [0,1] sub-scores) + `TrajectoryBundle` models, and a **frozen** `ReasoningBudget` (max_trajectories=3, refinement_rounds=2, search/traj=2, fetch/traj=6; `total_search_cap`/`total_fetch_cap` derived) the agent cannot self-raise. `reasoning/process_reward.py`: deterministic PPRM -- `SERVICE_WEIGHTS` (0.25 service_match + 0.25 quote_channel + 0.20 geo + 0.15 contact_reliability + 0.10 checklist + 0.05 evidence_diversity) and `ELECTRONICS_WEIGHTS` (0.30 fff + 0.20 lifecycle + 0.15 authorized + 0.15 stock + 0.10 datasheet + 0.10 risk), each summing to 1.0; `score(bundle)` returns reward + transparent breakdown with conflict/budget(/counterfeit for electronics) penalties subtracted; `score_all` ranks best-first. `reasoning/trajectory_policy.py`: `should_activate(mode, confidence, config)` (enabled + high-value mode + confidence < 0.75) + frozen `ReasoningConfig` carrying the budget. Built *around* gram_lite output, not replacing it. 10 new tests (test_process_reward.py, test_reasoning_budget.py). 239 pass. **Phase R (reasoning spine) begins.** |
| T-R.2 | DONE | **Trajectory runner + recursive repair + winner selection + controller wiring.** `reasoning/recursive_refiner.py`: `find_evidence_gaps(bundle)` (service: missing quote_channel/checklist/geo; electronics: FFF/datasheet; any: disputed -> price_conflict) + `build_repair_queries(gaps, seed)` (targeted templates). `reasoning/trajectory_runner.py`: `strategy_queries` (1-2 strategy-shaped queries/strategy, obsolescence vocab in the electronics ones), `TrajectoryRunner.run` (async; awaits a sync-or-async executor) runs <=max_trajectories trajectories, refines gaps in a bounded round 2 (`_merge` folds repair: metrics improve by max, evidence unions, rounds advance), scores via PPRM, selects the winner, emits a why-it-won/why-alternates-lost `explanation`, and **clamps per-round search/fetch so totals never exceed `search_ceiling`/`fetch_ceiling`** (= width x per-traj x rounds). `ReasoningBudget` gains `search_ceiling`/`fetch_ceiling` (12/36 at defaults). Controller gains opt-in `run_reasoning(query, mode)` (separate ledger/tracer; a per-trajectory `Budget` sized to the reasoning per-traj caps; reuses `_gather_queries`+ranker) + `_bundle_metrics` (ranked candidates -> normalized BundleMetrics + deduped evidence refs + disputed/conflict; evidence_diversity = distinct hosts/3). Every bundle ties its strategy/round to concrete ledger evidence ids (provenance via tracer `reasoning_trajectory` steps). Default `run` path unchanged (standalone + opt-in, deviation #4 pattern). Offline smoke: 3 trajectories, winner + explanation, within budget (8/12 search, 32/36 fetch), no network. 13 new tests (test_reasoning_runner.py, test_reasoning_controller.py, +2 in test_reasoning_budget.py). 249 pass. |
| T-4.3 | DONE | **Bi-temporal facts on the SQLite edge table (Zep/Graphiti dropped).** `graph/schema.py`: edges gains a `valid_to` column (event_ts = valid-from, ingest_ts = recorded-at, valid_to = closed-on-supersession / NULL = current) + a `CREATE_VIEWS_SQL` `edges_current` view (open rows). `graph/store.py`: `_migrate_valid_to` ALTERs pre-existing graph files before the view is created (fresh DBs get it from CREATE_SQL); `add_edge` gains `valid_to`; new `versions(src,dst,rel)` (all temporal versions, oldest valid-from first), `current_edges()` (via the view), `mark_superseded(...)` (closes open rows, `before` keeps a simultaneous fact open). New `memory/temporal.py` `BiTemporalGraph`: `record` closes prior open rows then appends the new edge (append-only supersession), `history`/`current`(max valid-from open row)/`as_of`(point-in-time). Reuses the existing engine -- no new dependency. Acceptance: price history returns both rows w/ timestamps; current = latest while the prior row is retained (edge_count stays 2); as_of returns the fact valid at a past instant; the current view excludes superseded rows. Existing graph/retrieve tests stay green (add_edge change is backward-compatible). 4 new tests (test_temporal.py). 253 pass. |
| T-R.3 | DONE | **Optional vector recall backend seam (auto-fallback).** New `memory/recall.py`: `RecallBackend` protocol; `LexicalRecallBackend` (default) delegates to the existing deterministic term-overlap `SemanticMemory.recall` (offline, zero-dep); `VectorRecallBackend` (optional) does `sqlite-vec` KNN over `text-embedding-v4` vectors (vec0 virtual table, serialized float32, distance->score). `sqlite_vec_available()` returns True only if `sqlite_vec` imports AND `enable_load_extension` is permitted. `build_recall_backend(memory, embedder=None, prefer_vector=True)` uses vectors ONLY when available + an embedder (API key) is supplied, and transparently falls back to lexical on missing extension, missing embedder, or any construction failure -- so the offline default never needs the extension. Decision rationale: keeps the search-quality upgrade (semantic recall when online) without the non-portable hard dependency (this dev box can load extensions, demo/CI may not). Acceptance: default backend is lexical and recalls offline; sqlite_vec_available() is a bool (False here, not installed); supplying an embedder still falls back to lexical when sqlite-vec is absent; a vector-backend construction error falls back. 4 new tests (test_recall_backend.py). 257 pass. |
| T-R.4 | DONE | **electronics_substitution vertical mode.** New first-class `ProcurementMode.ELECTRONICS_SUBSTITUTION` (+ success contract: substitute-with-evidence, FFF+lifecycle signal, no-drop-in-without-datasheet). Classifier gains a conservative electronics detector (component terms + substitute intent + obsolescence intent + an MPN regex) gated so it only fires on real substitute/obsolescence signals (or MPN+component term) -- existing service/product/contact classifications are unchanged (regression-guarded). Router routes it through the product extraction/ranker path (reuses the product budget, `produces_rfq=False`, `serendipity_mode=True`); `_is_validated` treats it like the product price rule; CLI `--mode` adds it. New `modes/electronics.py`: `InputPart` / `SubstituteCandidate` / `ElectronicsSubstitutionResult` schema + `build_electronics_result` with the hard safety rule -- a substitute is `recommended` (drop-in) ONLY with datasheet evidence + FFF >= 0.8 + active lifecycle, else `needs_engineering_review`; no evidence -> `rejected` and excluded; always emits `assumptions_and_limits`. Reasoning spine already routes the mode through electronics strategies + PPRM electronics weights. Offline smoke: hero query classifies as electronics_substitution; run() and run_reasoning() both execute without error (winner authorized_distributor_first). Additive -- deviation #11 mapping stays as fallback. 7 new tests (test_electronics.py). 264 pass. **Phase R (reasoning spine) complete.** |
