# Build Log — spider-qwen (SPIDER_QWEN_BUILD_PLAN execution)

One line per completed task. TDD: failing test(s) first, then implementation, then
the guardrail gate (offline e2e + full suite green) before commit.

| Task | Status | Notes |
|---|---|---|
| T-0 | DONE | `models:` block in policy_config.yaml (planner/extraction/embeddings/ocr) + `Policy.model_for(role)` with `SPIDER_QWEN_MODEL_<ROLE>` env overrides; legacy `qwen.*_model` keys and `QWEN_*` env still honoured. 9 new tests (test_models_config.py). Offline run green; 75 tests pass. |
| T-1.1 | DONE | `ranking/serendipity.py`: 4-slot `SerendipityResult` (primary_answer, s1_substitutes, s2_long_tail_sources, s3_risk_signals, evidence_refs, serendipity_score). Deterministic relevance x novelty x unexpectedness scoring vs top-3 baseline; `RiskSignal` model + `extra_risk_signals` seam for later phases. Wired into `RunResult.serendipity` via controller. 10 new tests; 85 pass. |
| T-1.2 | DONE | `serendipity/query_rewrite.py` `expand_query()` -> Step-Back + HyDE + Query2Doc + obsolescence-vocab + MPN-pattern + broker-operator variants (deterministic/offline; optional Qwen HyDE). `Planner.expand_query` passthrough. Golden test on "16-pin SMT op-amp TI..." passes. 6 new tests; 91 pass. |
| T-1.3 | DONE | `serendipity/corrective.py` CRAG `evaluate_retrieval()` (content-coverage + procurement-signal heuristic -> correct/ambiguous/incorrect) + page condensation; `corrective_queries()` (broker-first). Controller refactored to `_gather_queries`; on `incorrect` verdict it broaden/broker-pivots instead of answering. Tracer gains `detail`; metrics expose crag_verdict/confidence/corrective_searches. 7 new tests; 98 pass. |
| T-1.4 | DONE | `agent/compiler.py`: `TokenBucket` + `RateLimiter` (5 search/min, 25 fetch/min, injectable clock) + `LLMCompiler` DAG executor (level-parallel `asyncio.gather`, cycle/unknown-dep checks, per-kind peak-concurrency + DAG trace). Controller gains `rate_limiter`/`compiler` and `gather_parallel` (used by T-3.3) on a shared `_fetch_and_extract`. 8 new tests; 106 pass. **Phase 1 checkpoint:** offline hero query runs end-to-end (serendipity slots + CRAG verdict). Follow-up `fix(phase-1)`: wired query expansion (`merge_gather_queries` interleaves geo+expansion) and concurrent fetch (compiler DAG) into the **primary** gather path (were T-3.3-only); honest scaffold labeling for T-1.1 slots (s1/s2 are rank positions, not true S1/S2 classification). This routed the offline path through the rate-limited compiler and exposed a regression: the wall-clock `TokenBucket` (5 search/min) throttled the 80-case offline benchmark to ~45s/case (~1h, looked like a hang). Fix (SRP/DRY — offline-ness already expressed once, by the mock providers): providers carry `rate_limited` (mock=False; live TinyFish/Qwen=True); `Controller._build_rate_limiter()` picks `NullRateLimiter` when no provider draws a live quota, real `RateLimiter` otherwise (unknown providers default to throttled, protecting the live API). 2 regression tests (offline no-throttle timing guard + live-quota still-throttles). 165 pass. |
| T-2.1 | DONE | **Schema-constrained extraction + judge gate.** `tools/qwen_json_extractor.py`: re-prompt-on-malformed retry loop (`max_retries=2`) feeding the validation error back to qwen3.5-flash + retry telemetry (`calls/retries/malformed_final/last_attempts`, `retry_rate()`); malformed output recovers to a valid schema, post-retry malformed rate ~0. New `tools/page_judge.py`: Prometheus-style `PageJudge` scoring each fetched page on relevance (content-term coverage) x freshness (year recency) x source-authority (mfr/dist/aggregator/broker/marketplace/gov/business tiers) minus contradicts-ledger (disjoint price vs prior same-host item) -> `accept|flag|reject` (deterministic; optional Qwen `llm` override seam). Gate wired at the persistence boundary in `FetchService` (judge before `ledger.record`): reject -> dropped from results + not stored (recorded to errors), flag -> stored with `gate_status`/`judge` metadata + confidence 0.4, accept -> stored + metadata. Judges every page incl. empty-text (image-only low-authority pages cannot slip the gate). Controller gains `page_judge` param + `policy.qwen_page_judge_enabled()` (off by default; offline pipeline unchanged) + metrics `pages_rejected`/`pages_flagged`. Design note: judge lives in its own module and gates in FetchService (the page ledger-writer) rather than mutating `ledger.py` — the ledger already persists the verdict via existing metadata, so no `ledger.py` change was needed. 21 new tests (retry/judge/gate incl. controller e2e + empty-text regression); adversarial review (22 agents, 18 findings -> 1 confirmed+fixed). 127 pass. **Phase 2 begins.** Follow-up `fix(security)`: page judge LLM override hardened against prompt injection (untrusted page text) — override may only TIGHTEN the verdict, numeric fields type-checked + clamped, prompt isolates title/text in delimited blocks with refusal instructions; 4 tests. 131 pass. |
| T-2.2 | DONE | **Verification spine.** New `verification/atomic.py` (`decompose()` -> `AtomicClaim`s: vendor/price/moq/quote_channel/contact; critical flag on the mode-defining fact, conservative `not in _NON_PRICED` so a populated price with unknown status stays critical), `verification/minicheck.py` (MiniCheck entailment gatekeeper: value-grounding with currency/space/comma normalization + **whole-number numeric match** so "129" is not grounded by "1290"; token-overlap fallback; optional learned-NLI `model` seam, type-checked/clamped/never-raises), `verification/safe.py` (`SafeReverifier`: re-grounds flagged atoms against the wider evidence corpus + optional `search_fn`/TinyFish seam). `evidence/verifier.py` gains `VerificationSpine` (decompose -> MiniCheck -> SAFE): grounds each claim against the **source page text** (title+body via `parent_ledger_id`), **never the self-referential extraction snippet** (semantic_memory/synthetic refs return no premise -> forced through SAFE), writes `verified`/`verifier_score`/`verifier_stage` onto each claim ledger row; candidate `verified` iff all critical claims hold. Controller gains `verify`/`minicheck` params + `policy.verification_enabled()` (off by default; offline pipeline unchanged), blocks unverified candidates from `validated_candidates`, adds metrics `claims_verified`/`claims_unsupported`/`candidates_blocked_unverified`. Acceptance: injected fabricated price (S$999, not in page) -> critical price claim `verified=false` -> candidate blocked; paired no-verify test shows it would otherwise reach output. 22 new tests (atomic/minicheck/safe/spine + controller e2e + numeric-boundary/currency-regex/memory-self-reference/unknown-status regressions); adversarial review (36 agents, 30 findings -> 4 real bugs confirmed+fixed: numeric-substring bypass, semantic-memory self-grounding, price-criticality on unknown status, currency-regex mid-word). v1 limitations (documented, deferred): SAFE live-search + MiniCheck-FT5 model are opt-in seams (deterministic/offline default per project convention); currency/unit not yet separate critical claims; serendipity discovery view intentionally pre-verification (substitute verification is T-2.3/CoVe). 153 pass. Offline e2e green off + on (10/10 claims verified, 0 false blocks). Follow-up `fix(T-2.2)`: relation-grounding — vendor-scoped critical claims (price/moq/quote_channel + `contact_*`) now require the value and vendor to co-occur in one sentence (or the line above a price), so a competitor's price elsewhere on the page cannot verify the claim. `MiniCheck.check(subject=...)` + `_relation_grounded`; `VerificationSpine._minicheck_subject`; SAFE re-verify passes `claim.subject`. test_verification_spine extended. |
| T-2.3 | DONE | **CoVe + semantic entropy + contradiction.** New `verification/cove.py`: `SubstituteCandidate` (mpn/manufacturer/lifecycle_status/evidence_refs) + `ChainOfVerification` (draft -> fixed verify-questions answered against each candidate's evidence-grounded attributes, NOT the draft's self-assertion -> revise: drop any candidate failing a critical question) so a plausible-but-discontinued ("phantom") substitute is removed even at self-asserted confidence 0.99; `semantic_entropy(samples)` clusters on normalized (MPN, manufacturer) -> confidence = 1 - normalized entropy (agree=1.0, scatter~0.0); optional `sampler` seam draws N samples per candidate (Qwen generator) and gates on `min_confidence` (off by default). Contradiction -> `disputed` reuses semantic memory (DRY, per decision): `evidence/models.py` gains `DisputedAlternative` (value+confidence+spans); `memory/promotion.py` gains `contradicts()` (case/space/punct-normalized inequality; also fixes case-variant values being wrongly disputed); `memory/semantic.py` `SemanticFact` gains `entity_type="part"` + `disputed_alternatives`, and `upsert` now retains EVERY conflicting side (highest-confidence -> primary value/refs, rest -> alternatives) instead of discarding the loser. Acceptance: a Mouser-active vs DigiKey-NRND pair -> one `disputed` record keeping both values + both spans (survives reload); a phantom EOL substitute removed by CoVe. Standalone + opt-in (not wired into the controller yet; substitutes are populated by the parts graph/Wayback in T-3.x/T-5.x). 16 new tests (test_cove.py, test_contradiction.py). 181 pass. **T-2.4 next.** |
| T-2.4 | DONE | **Source-reliability prior + Merkle-DAG ledger.** New `governance/source_reliability.py` is the single home for source tiering: `classify_source(host)` (manufacturer/distributor/aggregator/broker/marketplace/government/business/unknown via suffix-matched domain lists) + `DEFAULT_RELIABILITY` priors (mfr 0.99, auth dist 0.95, aggregator 0.85, business 0.6, broker 0.5, unknown 0.4, marketplace 0.2) + `reliability_for(url, priors=)`. `tools/page_judge.py` now imports the shared classifier (DRY: removed its duplicate domain lists / `_classify_source` / `_host`; keeps its own gate `_AUTHORITY` weights). `evidence/models.py` `EvidenceItem` gains `reliability` + `parent_hash`/`chain_hash` + `content_digest()` (canonical digest over every field except the chain links) + `effective_confidence` property (= confidence x reliability; base confidence is never mutated). `evidence/ledger.py`: `record()` classifies the source, stores its reliability, then links each row `chain_hash = sha256(content_digest || parent_chain_hash)`; `verify_chain()` re-walks and flags the exact tampered/mis-linked `ledger_id`; `load()` resumes the chain tip so a later append continues the chain. Priors are policy-driven: `policy_config.yaml` `source_reliability:` block (overrides the code defaults) -> `Policy.source_reliability()` -> injected into the ledger by the controller. `evidence verify <run_id>` CLI now reports the chain re-walk (chain_ok/chain_checked/chain_issues) alongside span verification; exit 0 only if both hold. Acceptance: tampering one persisted row -> `verify_chain` not ok and points at that `ledger_id`; manufacturer effective confidence > marketplace. Cycle note: ledger imports `source_reliability` lazily inside `record()` (governance/__init__ pulls modes.contracts -> evidence.models). 9 new tests (test_reliability_ledger.py). 190 pass. **Phase 2 complete.** |
| T-3.1 | DONE | **Supplier-part property graph (SQLite LPG).** New `graph/` package: `schema.py` (two-table DDL -- `nodes(id,type,props)` + `edges(src,dst,rel,confidence,reliability,evidence_claim_id,event_ts,ingest_ts,grade,props)` with PK `(src,dst,rel,evidence_claim_id)` = one edge per asserting claim + src/dst indexes; node-type & relation-type tuples; canonical key helpers `part_key/mfr_key/dist_key`), `store.py` (`GraphStore` over stdlib `sqlite3`, file or `:memory:`; `upsert_node` merges props, `add_edge` rejects an empty evidence_claim_id (hard rule: never a bare URL) and dedups on the PK, `neighbors`, `traverse` = bounded multi-hop **recursive CTE**, `edges`/`node_count`/`edge_count`), `extract.py` (REBEL/SAC-KG shape: Generator = per-sentence relation-phrase + nearest-entity triples over MPN / manufacturer / distributor surfaces; Verifier = MiniCheck grounds each object surface in the source text (T-2.2 spine) so a hallucinated relation cannot upsert; Pruner = drop self-loops + duplicate triples; `ingest_text` upserts verified triples carrying the page's `evidence_claim_id` + source reliability). `evidence/graph.py` gains `render_property_graph(store)` Mermaid renderer (reuses `_node_id`/`_escape`). One embedded file -- no service, no Docker, `--offline` stays network-free; sqlite-vec embeddings can live in the same file (commented in the DDL). Cycle-safe: store imports only `schema` + stdlib. Standalone + opt-in (controller/retrieval integration is T-3.2). Acceptance: ingesting fixture datasheet prose builds a graph where the recursive-CTE traversal from `part:atmega48` resolves ACQUIRED_BY Microchip + SUPERSEDED_BY ATMEGA48A (+ CROSS_REFERENCE PIC16F88) within <=2 hops. 11 new tests (test_graph.py). 201 pass. **Phase 3 begins.** |
| T-3.2 | DONE | **HippoRAG PPR multi-hop retrieval.** New `graph/retrieve.py`: `personalized_pagerank(adjacency, seeds)` (pure-Python power iteration with restart/teleport to seeds + dangling-mass redistribution -- no scipy/numpy, keeps `--offline` light), `GraphRetriever` (seeds = query MPNs via `extract.find_mpns`; PPR over edges weighted by confidence x reliability; each result carries a Reasoning-on-Graphs relation path from a seed that doubles as the buyer-facing explanation), and `flat_vector_rank` (deliberately flat lexical baseline for the comparison). `extract.find_mpns` exposed (shared seed/extraction MPN detection, DRY). `controller.graph_retrieve(query, ledger)` builds the LPG from a run's ledger page text (every edge -> page `ledger_id` + source reliability) and runs PPR -- opt-in, not in the default pipeline. Acceptance: a multi-hop substitute query returns the successor with path "... -> SUPERSEDED_BY -> ..."; PPR recall@5 = 1.0 vs flat-baseline 0.0 on a 10-query fixture (successors are graph-adjacent but lexically dissimilar, so the flat baseline misses them). 4 new tests (test_graph_retrieve.py). 205 pass. |
